# Store LF in the repo AND check out LF in the working tree on every platform
# eol=lf overrides core.autocrlf, so a machine with autocrlf=true won't flip files to CRLF
* text=auto eol=lf

# Declare binary file types
*.spv binary
*.png binary
